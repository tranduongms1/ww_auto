import { test, expect } from 'fixtures';

const title = 'Verify Trade-up option should not displayed for Free gift';
const tag = ['@Core'];

test.use({ site: 'UK' });

test(title, { tag }, async ({ context, page, cart }) => {

});

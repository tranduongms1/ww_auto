import { test, expect } from 'fixtures';

const title = 'Verify the Galaxy Club is not displayed for FOC products';
const tag = ['@Tier2'];

test.use({ site: 'UK' });

test(title, { tag }, async ({ context, page, cart }) => {

});

import { test, expect } from 'fixtures';

const title = 'Verify Download data from MSA backoffice';
const tag = ['@Tier2'];

test.use({ site: 'UK' });

test(title, { tag }, async ({ context, page, cart }) => {

});

import { test, expect } from 'fixtures';

const title = 'Verify Safety Stock Limit Bulk Upload successfully';
const tag = ['@Tier2'];

test.use({ site: 'UK' });

test(title, { tag }, async ({ context, page, cart }) => {

});

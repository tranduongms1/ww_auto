import { test, expect } from 'fixtures';

const title = 'Backoffice "forgot password" flow should work as it is without any change';
const tag = ['@Core'];

test.use({ site: 'UK' });

test(title, { tag }, async ({ context, page, cart }) => {

});

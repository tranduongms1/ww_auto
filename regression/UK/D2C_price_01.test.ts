import { test, expect } from 'fixtures';

const title = 'Verify configuration of enableD2Cpricing and properties';
const tag = ['@Tier2'];

test.use({ site: 'UK' });

test(title, { tag }, async ({ context, page, cart }) => {

});
